import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as BackTopStories from "./BackTop.stories";

<Meta of={BackTopStories} />

# BackTop

A button that appears when the page is scrolled and allows the user to quickly return to the top.

## When To Use

- When the page is very long and needs a quick way to return to the top.
- When the user needs to frequently scroll back to the top for navigation or other actions.

## Examples

### Basic
The button appears after scrolling 400px.

<Canvas of={BackTopStories.Basic} />

### Custom Element
You can customize the content of the BackTop button.

<Canvas of={BackTopStories.CustomElement} />

### Specific Target
You can specify a target container for the scroll event.

<Canvas of={BackTopStories.SpecificTarget} />

## Props

<Controls of={BackTopStories.Basic} />
