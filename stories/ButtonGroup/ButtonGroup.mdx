import {
  Meta,
  Title,
  Description,
  Primary,
  Controls,
  Stories,
  Canvas,
} from "@storybook/blocks";
import * as ButtonGroupStories from "./ButtonGroup.stories";
import { T } from "../T";

<Meta of={ButtonGroupStories} />

<Title />
<p
  style={{
    margin: "0 0 16px 0",
    fontSize: "18px",
    lineHeight: "28px",
    color: "#6c757d",
  }}
>
  ButtonGroupコンポーネントは、複数のボタンをグループ化して表示するために使用します。
</p>
<Description />

## 基本的な使い方

ボタンを横並びに配置します。`gap`プロパティでボタン間の間隔を調整できます。

<Canvas of={ButtonGroupStories.MediumGroup} />

## サイズ展開

Large, Medium, Smallのボタンをそれぞれ配置した場合の例です。

### Large

<Canvas of={ButtonGroupStories.LargeGroup} />

### Medium

<Canvas of={ButtonGroupStories.MediumGroup} />

### Small

<Canvas of={ButtonGroupStories.SmallGroup} />

## 結合モード

`joined`プロパティを`true`に設定すると、ボタンが隙間なく結合されます。
このモードでは、中間のボタンの角丸が削除され、一体感のあるデザインになります。

<Canvas of={ButtonGroupStories.JoinedGroup} />

### 優先度の強制 (Priority Override)

`priority` プロパティを指定することで、グループ内のすべてのボタンの `priority` を統一できます。
特に `joined` モードを使用する場合、異なる優先度のボタンが混在するとデザインが崩れる可能性があるため、このプロパティを使用して統一することを推奨します。

> [!IMPORTANT]
> UX上の注意点
> UIデザインの原則として、1つの画面（または1つのコンテキスト）におけるプライマリボタンは原則として1つのみにすることが推奨されます。
> `priority="primary"` を指定して複数のボタンを並べると、ユーザーがどの操作を優先すべきか迷う原因（認知負荷の増大）になります。
>
> グループ全体の優先度を統一する場合は、通常 `secondary` や `tertiary` を選択することを推奨します。`primary` への統一は、技術的に可能ですが、慎重に検討してください。

<Canvas of={ButtonGroupStories.JoinedGroupPrimary} />

<Controls />
