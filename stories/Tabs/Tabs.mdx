import { Meta, Canvas, Controls} from "@storybook/blocks";
import * as TabsStories from "./Tabs.stories";
import { T } from "../T";

<Meta of={TabsStories} />

# Tabs

<p style={{ margin: "0 0 16px 0", fontSize: "18px", lineHeight: "28px", color: "#6c757d" }}>
  <T k="doc_tabs_title" />
</p>

コンテンツをグループ化し、表示を切り替えるために使用。

<Canvas of={TabsStories.Default} />

<Controls of={TabsStories.Default} />

## 垂直方向への配置

`orientation="vertical"`を指定することで、タブを垂直方向に並べることが可能です。

<Canvas of={TabsStories.Vertical} />

## スクロール可能なタブ

タブの数が多い場合やラベルが長い場合、タブリストを横方向にスクロールすることが可能です。

<Canvas of={TabsStories.Scrolling} />

