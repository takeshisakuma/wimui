import { Meta, Canvas, Controls, Story } from "@storybook/addon-docs/blocks";
import * as LabelStories from "./Label.stories";
import { T } from "../T";

<Meta of={LabelStories} />

# Label

<p
  style={{
    margin: "0 0 16px 0",
    fontSize: "18px",
    lineHeight: "28px",
    color: "#6c757d",
  }}
>
  <T k="doc_label_title" />
</p>

フォーム要素の説明やキャプションを表示するためのコンポーネントです。

## 基本的な使い方

<Canvas of={LabelStories.Default} />

## 必須・任意表示

`required` プロパティで必須マーク、`showOptional` プロパティで任意マークを表示できます。

### Required

<Canvas of={LabelStories.Required} />

### Optional

<Canvas of={LabelStories.Optional} />

## Props

<Controls />
