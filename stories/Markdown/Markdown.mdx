import {
  Meta,
  Title,
  Description,
  Primary,
  Controls,
  Stories,
  Canvas,
} from "@storybook/addon-docs/blocks";
import * as MarkdownStories from "./Markdown.stories";
import { T } from "../T";

<Meta of={MarkdownStories} />

<Title />
<p
  style={{
    margin: "0 0 16px 0",
    fontSize: "18px",
    lineHeight: "28px",
    color: "#6c757d",
  }}
>
  <T k="doc_markdown_desc" />
</p>
<Description />

<Canvas of={MarkdownStories.Default} />
<Controls />

## 概要

Markdown（マークダウン）コンポーネントは、Markdown形式で記述されたテキストを、適切なHTMLとしてレンダリングするために使用されます。
ブログ記事、ドキュメント、ユーザー生成コンテンツのプレビューなどに適しています。

## 特徴

<table>
  <thead>
    <tr>
      <th>特徴</th>
      <th>詳細</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>GFM (GitHub Flavored Markdown) サポート</strong></td>
      <td>テーブル、打ち消し線、チェックリストなどが利用可能です。</td>
    </tr>
    <tr>
      <td><strong>スタイリング</strong></td>
      <td>WIM UI のデザインシステムに合わせたデフォルトスタイルが適用されます。</td>
    </tr>
  </tbody>
</table>

## 注意点

<table>
  <thead>
    <tr>
      <th>項目</th>
      <th>内容</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>セキュリティ</strong></td>
      <td>内部で <code>react-markdown</code> を使用しており、デフォルトでXSS攻撃を防止するように設計されていますが、複雑なHTML許可設定などを行う場合は注意が必要です。</td>
    </tr>
  </tbody>
</table>
