import {
  Meta, Title, Description, Primary, Controls, Stories, Canvas } from "@storybook/blocks";
import * as MentionsStories from "./Mentions.stories";
import { T } from "../T";

<Meta of={MentionsStories} />

<Title />
<p style={{ margin: "0 0 16px 0", fontSize: "18px", lineHeight: "28px", color: "#6c757d" }}>
  テキスト入力中に特定の文字（@など）をトリガーにして候補を表示し、選択できるようにするコンポーネントです。
</p>

## 基本的な使い方

`options` プロパティに候補リストを、`trigger` プロパティに開始文字（デフォルトは `@`）を指定します。

<Canvas of={MentionsStories.Default} />

## カスタマイズ

### 別のトリガー文字

`trigger` を変更することで、ハッシュタグ（#）やコマンド（/）などの入力をサポートできます。

<Canvas of={MentionsStories.CustomTrigger} />

<Controls />

