import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import { T } from "../T";
import * as MultiSelectStories from "./MultiSelect.stories";

<Meta of={MultiSelectStories} />

# MultiSelect

<p
  style={{
    margin: "0 0 16px 0",
    fontSize: "18px",
    lineHeight: "28px",
    color: "#6c757d",
  }}
>
  <T k="doc_multiselect_title" />
</p>

ユーザーが定義済みの選択肢から複数を選択するためのドロップダウンメニューです。

## Usage

```jsx
import { MultiSelect } from "@/components/MultiSelect/MultiSelect";

const options = [
  { label: "Apple", value: "apple" },
  { label: "Banana", value: "banana" },
  { label: "Orange", value: "orange" },
];

<MultiSelect
  options={options}
  onChange={(vals) => console.log(vals)} // returns string[]
/>;
```

## Examples

### Default

<Canvas of={MultiSelectStories.Default} />

### With Label

<Canvas of={MultiSelectStories.WithLabel} />

### Multiple Selected

<Canvas of={MultiSelectStories.MultipleSelected} />

### Disabled

<Canvas of={MultiSelectStories.Disabled} />

## Props

<Controls />
