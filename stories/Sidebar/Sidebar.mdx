import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as SidebarStories from "./Sidebar.stories";
import { T } from "../T";

<Meta of={SidebarStories} />

# Sidebar

<p
  style={{
    margin: "0 0 16px 0",
    fontSize: "18px",
    lineHeight: "28px",
    color: "#6c757d",
  }}
>
  <T k="doc_sidebar_title" />
</p>

Sidebarは、ページの横側に配置されるナビゲーションや情報のパネルです。

> **PCのみ**: このコンポーネントは主にPC（マウス・キーボード操作）向けに設計されています。スマートフォンなどの小画面デバイスでは、`TabBar`やドロワーメニューなどの代替手段を検討してください。

> 📘 Guide: See [Guides / App Layout](?path=/docs/guides-app-layout--docs) for layout patterns.

## インポート

```jsx
import Sidebar from "@/components/Sidebar/Sidebar";
import { T } from "../T";
```

## 基本的な使用方法

`Sidebar` の中に `Sidebar.Header`, `Sidebar.Content`, `Sidebar.Footer` を配置し、`Content` の中に `Sidebar.Item` を配置します。

<Canvas of={SidebarStories.Default} />

## 折りたたみ状態 (Collapsed)

`collapsed` プロパティを使用すると、サイドバーを最小サイズに折りたたみます。アイコンのみが表示されるようになります。

<Canvas of={SidebarStories.Collapsed} />

## カスタム幅

`width` プロパティを使用して、展開時の幅を調整できます。

<Canvas of={SidebarStories.CustomWidth} />

## API リファレンス

<Controls />
