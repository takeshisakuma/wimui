import { Meta, Canvas, Controls, Title, Primary, Stories } from "@storybook/addon-docs/blocks";
import * as ToastStories from "./Toast.stories";
import { T } from "../T";

<Meta of={ToastStories} />

<Title />

<p
  style={{
    margin: "0 0 16px 0",
    fontSize: "18px",
    lineHeight: "28px",
    color: "#6c757d",
  }}
>
  <T k="doc_toast_title" />
</p>

<p><T k="doc_toast_long_desc" /></p>

## <T k="doc_basic_usage" />

<p><T k="doc_toast_basic_usage_desc" /></p>

<Canvas of={ToastStories.Default} />

## <T k="doc_toast_system_usage" />

<p><T k="doc_toast_system_usage_desc" /></p>

```tsx
import { ToastProvider, useToast } from "./Toast";
import { T } from "../T";

const App = () => (
  <ToastProvider position="bottom-right">
    <MyComponent />
  </ToastProvider>
);

const MyComponent = () => {
  const { show } = useToast();

  return (
    <button
      onClick={() =>
        show({
          title: "Success",
          description: "Data has been successfully saved.",
          variant: "success",
        })
      }
    >
      Show Toast
    </button>
  );
};
```

### <T k="doc_toast_interactive_example" />

<p><T k="doc_toast_interactive_example_desc" /></p>

<Canvas of={ToastStories.Interactive} />

## <T k="doc_variations" />

<p><T k="doc_toast_variants_desc" /></p>

### <T k="doc_info" />

<Canvas of={ToastStories.Info} />

### <T k="doc_success" />

<Canvas of={ToastStories.Success} />

### <T k="doc_warning" />

<Canvas of={ToastStories.Warning} />

### <T k="doc_error" />

<Canvas of={ToastStories.ErrorStatus} />

## <T k="doc_props" />

### <T k="doc_toast_props" />

<Controls />

### <T k="doc_toast_provider_props" />

| Prop       | Type            | Default       | Description                                                                                                |
| :--------- | :-------------- | :------------ | :--------------------------------------------------------------------------------------------------------- |
| `children` | `ReactNode`     | -             | Children elements to be rendered within the provider                                                                           |
| `position` | `ToastPosition` | `"top-right"` | Toast display position (`top-right`, `top-left`, `top-center`, `bottom-right`, `bottom-left`, `bottom-center`) |
| `duration` | `number`        | `3000`        | Duration for which the toast is displayed (in milliseconds)                                                                         |

<Stories />
