import {
  Meta, Title, Description, Primary, Controls, Stories, Canvas } from "@storybook/blocks";
import * as ListStories from "./List.stories";
import { T } from "../T";

<Meta of={ListStories} />

<Title />
<p style={{ margin: "0 0 16px 0", fontSize: "18px", lineHeight: "28px", color: "#6c757d" }}>
  関連するアイテムを整理して表示するための汎用的なリストコンポーネント
</p>

<Description />

<h2>バリエーション</h2>

<h3>箇条書き (Unordered List)</h3>
<Canvas of={ListStories.Unordered} />

<h3>番号付き (Ordered List)</h3>
<Canvas of={ListStories.Ordered} />

<h3>サイズ</h3>
<p>small, medium (default), large の3つのサイズをサポートしています。</p>
<Canvas of={ListStories.Small} />
<Canvas of={ListStories.Large} />

<h3>スペーシング</h3>
<p>アイテム間の余白を tight, normal (default), loose から選択できます。</p>
<Canvas of={ListStories.LooseSpacing} />

<h3>アイコン付き</h3>
<p>ListItem に <code>iconName</code> を指定することで、アイコンを表示できます。<code>iconPosition</code> で左右の配置を選択可能です。</p>
<Canvas of={ListStories.WithIcons} />


<Controls />

