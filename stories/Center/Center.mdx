import { Canvas, Meta, Controls} from "@storybook/blocks";
import * as CenterStories from "./Center.stories";
import { T } from "../T";

<Meta of={CenterStories} />

# Center

<p style={{ margin: "0 0 16px 0", fontSize: "18px", lineHeight: "28px", color: "#6c757d" }}>
  <T k="doc_center_title" />
</p>

<T k="doc_center_title" />

`Center` コンポーネントは、子要素を垂直および水平方向の中央に配置するために使用されます。

## 使い方

```tsx
import { Center } from "@/components/Center/Center";

const Demo = () => (
  <Center h={200} bg="#f0f0f0">
    中央に配置されるコンテンツ
  </Center>
);
```

<Canvas of={CenterStories.Default} />

## 特徴

- **完全な中央配置**: `align-items: center` と `justify-content: center` がデフォルトで適用されます。
- **インラインモード**: `inline` プロパティを使用することで、`inline-flex` として動作させることができます。
- **柔軟なサイズ**: `Box` や `Flex` のプロパティを継承しているため、高さ (`h`) や幅 (`w`) も簡単に指定できます。

## API リファレンス

<Controls />

## インライン配置

`inline` プロパティを使用すると、テキストの流れの中に配置できます。

<Canvas of={CenterStories.Inline} />

## アイコンの中央配置

アイコンボタンや丸いバッジなどの作成に便利です。

<Canvas of={CenterStories.Icons} />
