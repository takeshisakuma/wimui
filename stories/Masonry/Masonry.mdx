import { Canvas, Meta, Controls } from "@storybook/addon-docs/blocks";
import * as MasonryStories from "./Masonry.stories";
import { T } from "../T";

<Meta of={MasonryStories} />

# Masonry

<p
  style={{
    margin: "0 0 16px 0",
    fontSize: "18px",
    lineHeight: "28px",
    color: "#6c757d",
  }}
>
  <T k="doc_masonry_title" />
</p>



`Masonry` は、要素の高さが異なる場合でも、隙間を埋めるように効率的に配置するレイアウトコンポーネントです。
主に画像ギャラリーや、高さの異なるカードを並べる際に使用されます。

## 使い方

```tsx
import { Masonry } from "@/components/Masonry/Masonry";
import { Box } from "../Box/Box";

const Demo = () => (
  <Masonry columns={3} spacing={16}>
    <Box h={150} bg="lightblue">
      1
    </Box>
    <Box h={200} bg="lightgreen">
      2
    </Box>
    <Box h={100} bg="lightpink">
      3
    </Box>
    <Box h={250} bg="lightyellow">
      4
    </Box>
  </Masonry>
);
```

<Canvas of={MasonryStories.Default} />

## 特徴

<table>
  <thead>
    <tr>
      <th>特徴</th>
      <th>説明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>自動配置</strong></td>
      <td>CSSの <code>column-count</code> を使用して、要素を自動的にカラムに振り分けます。</td>
    </tr>
    <tr>
      <td><strong>レスポンシブな間隔</strong></td>
      <td><code>spacing</code> プロパティで要素間の余白を簡単に調整できます。</td>
    </tr>
    <tr>
      <td><strong>フレキシブルなカラム数</strong></td>
      <td><code>columns</code> プロパティで任意のカラム数を指定可能です。</td>
    </tr>
  </tbody>
</table>

## API リファレンス

<Controls />

## カラムの調整

カラム数を変更することで、レイアウトの密度を調整できます。

<Canvas of={MasonryStories.ManyColumns} />

## スペーシングの調整

`spacing` プロパティを使用して、要素間の距離を調整できます。

<Canvas of={MasonryStories.LargeSpacing} />
