import { Meta, Canvas, Controls, Story} from "@storybook/blocks";
import * as OtpInputStories from "./OtpInput.stories";
import { T } from "../T";

<Meta of={OtpInputStories} />

# OtpInput

<p style={{ margin: "0 0 16px 0", fontSize: "18px", lineHeight: "28px", color: "#6c757d" }}>
  <T k="doc_otpinput_title" />
</p>

ワンタイムパスワード（OTP）やPINコードを入力するための分割された入力フィールドです。

## 基本的な使い方

<Canvas of={OtpInputStories.Default} />

## 桁数変更

`length` プロパティで入力フィールドの数を変更できます。

<Canvas of={OtpInputStories.Length6} />

## 状態

### Error

<Canvas of={OtpInputStories.Error} />

### Disabled

<Canvas of={OtpInputStories.Disabled} />

## Props

<Controls />

