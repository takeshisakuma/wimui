import {
  Meta,
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  Stories,
  Canvas,
} from "@storybook/blocks";
import * as CodeStories from "./Code.stories";

<Meta of={CodeStories} />

<Title />
<Subtitle>
  インラインコードやコードブロックを表示するために使用される
</Subtitle>

## 使用法

```tsx
import { Code } from './Code';

// インライン
<Code>npm install wimui</Code>

// ブロック
<Code block language="bash">
npm install wimui
</Code>
```

<Canvas of={CodeStories.Inline} />

<Controls />

## バリエーション

### Inline

<Canvas of={CodeStories.Inline} />

### Block

<Canvas of={CodeStories.Block} />

### Long Content

<Canvas of={CodeStories.LongContent} />

> **Note**
> `Code` コンポーネントのコードブロック（`block` プロパティが `true` の場合）では、横スクロールを有効にしている<br />
これは、長いコード行を無理に折り返すとインデントや構造が崩れて可読性が低下するため


