---
"wimui": minor
---

## 既定値の変更（利用者の画面が黙って変わる）

`Gallery` で選択中の写真を拡大しないようにしました。以前は選択中もホバーと同じ `scale(1.04)` で拡大していて、選択中の合図が「枠線・角丸・拡大」の 3 つ重なっていました。そのうえ、選んでいない写真にカーソルを乗せると、写真の部分は選択中と見分けがつきませんでした。

選択中の合図は枠線（と角丸）だけになります。ホバーの拡大はこれまでどおりです（枠の中で写真が寄るだけで、タイルの位置と大きさは変わりません）。props は変わりません。
