---
"wimui": patch
---

`Dashboard` のスタイルを、他と同じ `@layer component` の中へ入れた（T243 の第 6 弾）。

`Dashboard` はカスケードレイヤーの外にあったため、**利用者が書いた CSS が `Dashboard` の内部スタイルに勝てなかった**。

**描画は変わらない**（`Dashboard` とレイアウト・パターンの 109 ストーリーを撮り比べて差分ゼロ）。

> **上書きしている場合は確認を。** `Dashboard` を自前の CSS で上書きしていた場合、**これまで効かなかった上書きが効くようになる**。
