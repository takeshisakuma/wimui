---
"wimui": patch
---

Size `ScheduleView` to its slots when the parent has no height, make overflowing scrollers keyboard reachable, give default events AA contrast, theme FullCalendar's page background, and wrap the toolbar by group as width allows.
