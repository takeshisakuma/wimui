@use "../../../base" as *;

.wim-tree-select-container {
  width: 100%;
  container-type: inline-size;
}

.wim-tree-select {
  position: relative;
  align-self: flex-start; // prevent flex-stretch from pushing to flex-end when max-width kicks in
  width: 100%;
  min-width: 200px;
  max-width: var(--wim-input-width, 280px);
  font-family: var(--wim-font-family);

  @container (max-width: 480px) {
    min-width: 160px;
  }

  &--full-width {
    min-width: 0;
    max-width: none;
  }

  &__label {
    display: block;
    margin-bottom: 8px;
    font-size: 14px;
    font-weight: 500;
    color: var(--wim-color-text-primary);
  }

  &__trigger {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    min-height: 40px;
    padding: 8px 12px;
    cursor: pointer;
    background: transparent;
    border: none;
    border-radius: 0;
    transition: var(--wim-transition-fast);

    &:hover:not(&--disabled) {
      border-color: var(--wim-color-primary);
    }

    &:focus:not(&--disabled) {
      outline: none; // フォーカスリングは InputBase の :focus-within で提供
    }

    &:focus-visible:not(&--disabled) {
      @media (forced-colors: active) {
        outline: 2px solid Highlight;
        outline-offset: 2px;
      }
    }

    &--open {
      border-color: var(--wim-color-primary);
    }

    &--disabled {
      color: var(--wim-color-text-on-disabled);
      pointer-events: none;
      cursor: not-allowed;

      // background / border / opacity are handled by the InputBase wrapper
    }
  }

  &__value {
    flex: 1;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    font-size: 14px;
    color: var(--wim-color-text-primary);
    white-space: nowrap;

    &--placeholder {
      color: var(--wim-color-text-placeholder);
    }
  }


  &__icon {
    display: flex;
    align-items: center;
    color: var(--wim-color-text-secondary);
  }

  &__clear {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4px;
    color: var(--wim-color-text-secondary);
    cursor: pointer;
    background: none;
    border: none;
    border-radius: 4px;
    transition: var(--wim-transition-fast);
  }

  &__dropdown {
    position: absolute;
    top: 100%;
    left: 0;
    z-index: 1000;
    width: max-content;
    min-width: 100%;
    max-width: min(480px, calc(100vw - 32px));
    max-height: 320px;
    padding: 4px 0; // Provide space for focus ring shadows
    margin-top: 4px;
    overflow: hidden auto; // Force vertical scroll only
    background: var(--wim-color-bg-component);
    border: 1px solid var(--wim-color-border);
    border-radius: 8px;
    box-shadow: var(--wim-shadow-md);

    @media (forced-colors: active) {
      border: 2px solid ButtonBorder;
      box-shadow: none;
    }

    &::-webkit-scrollbar {
      width: 6px;
    }

    &::-webkit-scrollbar-thumb {
      background: var(--wim-color-border);
      border-radius: 3px;
    }
  }

  // Adapt TreeView styles for Selectbox dropdown
  .wim-tree-view {
    // width: max-content: sizes to widest item label, allowing dropdown to
    // exceed trigger width.
    // min-width: 100%: stretches tree-view to fill dropdown when trigger is
    // wider than all items (e.g. full-width variant). Percentage min-width
    // is excluded from intrinsic size calculations, so no circular dependency
    // with __dropdown { width: max-content }.
    width: max-content;
    min-width: 100%;
    padding: 4px 0;

    // overflow: hidden in tree-view.scss clips items when width is max-content.
    // __dropdown handles overflow; tree-view should not clip internally.
    overflow: visible;
    border: none;
    box-shadow: none; // __dropdown provides shadow

    // container-type: inline-size on items excludes them from the parent's
    // max-content width calculation, collapsing items to 0px.
    // Reset to normal so items contribute their text width correctly.
    .wim-tree-view-item {
      container-type: normal;
    }

    &__search {
      padding: 4px 12px;
      margin-bottom: 8px;
    }
  }
}
