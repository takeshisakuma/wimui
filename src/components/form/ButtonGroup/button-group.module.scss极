@use "../../../tokens/breakpoints" as *;

.wim-button-group {
  display: flex;
  flex-wrap: wrap;
  row-gap: var(--wim-spacing-sm);
  align-items: center;
  justify-content: center;
  width: 100%;

  &.wim-button-group--joined {
    gap: 0;

    .wim-button {
      flex: 1 1 auto;
      border-radius: 0;

      &:not(:first-child) {
        margin-left: -2px; // Overlap borders horizontally
      }

      &:first-child {
        border-top-left-radius: var(--wim-radius-md);
        border-bottom-left-radius: var(--wim-radius-md);
      }

      &:last-child {
        border-top-right-radius: var(--wim-radius-md);
        border-bottom-right-radius: var(--wim-radius-md);
      }

      // Handle hover/focus state overlap
      // Button は @layer component 内のため非レイヤーの ButtonGroup が自動で勝つ
      &:hover,
      &:focus-visible {
        z-index: 1;
        transform: none;
      }

      &:active {
        transform: none;
      }
    }

    // 縦並びへの切り替え（flex: 1 1 100% で各ボタンを1行に押し込み、連結状態を維持する）
    // :not(:first-child) を追加して特異性を 0,4,0 に揃え、同名ルール（0,4,0）に対してソース順で勝つ
    @include down(sm) {
      .wim-button {
        flex: 1 1 100%;

        &:not(:first-child) {
          margin-top: -2px; // Overlap borders vertically
          margin-left: 0;
        }

        &:first-child {
          border-radius: var(--wim-radius-md) var(--wim-radius-md) 0 0;
        }

        &:last-child {
          border-radius: 0 0 var(--wim-radius-md) var(--wim-radius-md);
        }

        // 1つしかない場合のケア
        &:first-child:last-child {
          border-radius: var(--wim-radius-md);
        }
      }
    }
  }
}
