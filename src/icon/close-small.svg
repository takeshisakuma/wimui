<!-- **非推奨。`CloseIcon` を使ってください。** 2026-08-05 に close.svg と統合した。
     元は viewBox 15×15 の塗りパス（Radix 系）で、docs には「小さい場所での視認性の
     ために使い分ける」と書かれていたが、①24 / 16 / 14px の 3 サイズで並べても
     見分けがつかず ②その分担をコードが守っていなかった（`Tag` も `Chip` も
     `CloseIcon` を使っている）。export の削除は破壊的変更なので、互換のために
     同じ字形を残す。次の major で外す。T77 -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <line x1="18" y1="6" x2="6" y2="18"></line>
    <line x1="6" y1="6" x2="18" y2="18"></line>
</svg>
