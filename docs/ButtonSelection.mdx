import { Meta} from "@storybook/blocks";

<Meta title="Guides/Buttons & Actions" />

# ボタンコンポーネントの使い分け

ユーザーのアクションを促すボタンは、その重要度や役割に応じて視覚的な重み付けを使い分ける必要があります。

## Link vs Button

セマンティクス（意味付け）とアクセシビリティを正しく保つために、見た目ではなく「目的」で使い分けます。
詳細な使い分け基準については、専用のガイドを参照してください。

> 📘 **Guide**: **[Link vs Button (リンクとボタンの使い分け)](?path=/docs/guides-link-vs-button--docs)**

---

## 役割と優先度

<table>
  <thead>
    <tr>
      <th>種類 (Priority)</th>
      <th>視覚的な重み</th>
      <th>用途</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Primary</strong></td>
      <td>強 (塗りつぶし)</td>
      <td>その画面で最も重要な単一のアクション（送信、保存、完了）</td>
    </tr>
    <tr>
      <td><strong>Secondary</strong></td>
      <td>中 (アウトライン)</td>
      <td>主要アクションを補完するアクション（キャンセル、編集、戻る）</td>
    </tr>
    <tr>
      <td><strong>Tertiary (Ghost)</strong></td>
      <td>弱 (テキストのみ)</td>
      <td>重要度の低いオプション、多用しても画面を圧搬しない</td>
    </tr>
    <tr>
      <td><strong>Danger</strong></td>
      <td>強 (赤/警告色)</td>
      <td>取り返しのつかない操作、注意が必要な操作（削除、退会）</td>
    </tr>
  </tbody>
</table>

---

## 特殊なボタン

### 1. Fab (Floating Action Button)
画面の右下などに浮いているボタンです。
- いつ使うか: 画面内のどこからでもアクセスしたい、そのページで最も頻繁に行われるアクション。

### 2. Button Group
複数のボタンを繋げて表示するコンポーネントです。
- いつ使うか: 関連性の高い一連のアクションをまとめるとき（例：ソート順の切り替え）。

### 3. Copy Button
値をクリップボードにコピーするための専用ボタンです。
- いつ使うか: ユーザーが手動で選択してコピーするのが難しい値。

---

## ガイドライン
- 一貫性: 同じアクションには同じ種類のボタンを常に使用してください。
- 配置: 主要なボタン（Primary）は、一般的な視線移動の終点に配置します。
- 配置の競合: 1つの画面に Primary ボタンは原則として1つだけに留めます。

